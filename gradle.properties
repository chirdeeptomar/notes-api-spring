org.gradle.jvmargs=-Xmx2g

# This machine's default JDK (25) is newer than Gradle 8.14's bundled Kotlin compiler supports
# for parsing build.gradle.kts, so the Gradle daemon itself must be launched on Java 21 -
# a toolchain block in build.gradle.kts only selects the JDK for compiling/running project
# sources, it cannot rescue the Gradle process itself from crashing on script compilation.
org.gradle.java.home=/Users/chirdeeptomar/.sdkman/candidates/java/21.0.12+1.1-zulu
